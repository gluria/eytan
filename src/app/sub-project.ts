import { subProjectImages } from './sub-project-images';

export class subProject {
  subProjectName: string;
  subProjectHebrewName: string;
  subProjectLocation: string;
  subProjectYear: string;
  subProjectStatus: string;
  subProjectImages: subProjectImages[];
}
